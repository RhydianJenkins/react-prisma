version: '3.7'

services:
  react-ui:
    container_name: react-ui
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
    ports:
      - '3000:3000'
    command: sh -c "npm install && npm run start"
    networks:
      - react-prisma

networks:
  react-prisma:
